{
  "name": "songzy-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'Static site — no build needed'",
    "dev": "python3 -m http.server 8000"
  },
  "dependencies": {
    "stripe": "^17.0.0",
    "resend": "^4.0.0",
    "@vercel/postgres": "^0.10.0",
    "@vercel/blob": "^0.27.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20"
  }
}
