{
  "name": "songzy-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-landing-pages.mjs && echo 'Static site + generated landing pages'",
    "dev": "python3 -m http.server 8000",
    "migrate": "node scripts/migrate.mjs",
    "build-landings": "node scripts/build-landing-pages.mjs",
    "generate-blog": "node scripts/generate-blog-posts.mjs"
  },
  "dependencies": {
    "stripe": "^17.0.0",
    "resend": "^4.0.0",
    "@vercel/postgres": "^0.10.0",
    "@vercel/blob": "^0.27.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20"
  }
}
