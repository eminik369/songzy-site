# Voice demos

Place MP3 demos here using these exact filenames:

- `sofia.mp3`
- `matteo.mp3`
- `giada.mp3`
- `leonardo.mp3`
- `aurora.mp3`


- `nico.mp3`

Each demo should be 10–20s, representative of the voice style listed in `checkout.html` (step 4 voice picker).

Also drop square-crop photos at:

- `../voices-photos/sofia.jpg` … etc.

Then update `<img src>` paths in `checkout.html` step 4 to `/assets/voices-photos/<name>.jpg` (currently using Unsplash placeholders).
